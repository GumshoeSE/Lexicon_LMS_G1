@model Lexicon_LMS_G1.Models.StudentViewCourseViewModel

@{
    ViewData["Title"] = "Course";
}

<h1>Course</h1>

<div class="container">
    <div class="row">
        <div class="col-6" id="assignments">
            <ul class="list-group">
                @foreach (var assignment in Model!.Assignments)
                {
                    <li class="list-group-item">
                        @Html.DisplayFor(a => assignment.Name)
                    </li>
                }
            </ul>
        </div>
        <div class="col-6" id="modules">
            <ul class="list-group">
                @foreach (var assignment in Model!.Modules)
                {
                    <li class="list-group-item">
                        @Html.DisplayFor(a => assignment.Name)
                    </li>
                }
            </ul>
        </div>
    </div>
    <div class="row">
        <div class="col-6" id="attendeesbutton">

        </div>
    </div>
</div>